using UnityEngine;

[CreateAssetMenu(fileName = "SpawnInfo", menuName = "Scriptable Object/SpawnInfo", order = int.MaxValue)]
public class SpawnInfo : ScriptableObject
{
    public float roundCurTime;
}